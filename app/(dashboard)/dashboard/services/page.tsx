import { ComingSoonPage } from "@/components/shared/coming-soon";

export default function ServicesPage(): React.JSX.Element {
  return (
    <ComingSoonPage
      title="სერვისები"
      body="პროვაიდერის სერვისების მართვა — შემდეგ ეტაპზე."
    />
  );
}
